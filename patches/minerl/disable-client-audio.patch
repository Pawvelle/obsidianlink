--- a/launchClient.sh
+++ b/launchClient.sh
@@ -49,6 +49,8 @@
 fi
 
 fatjar=build/libs/mcprec-6.13.jar
-java -Xmx$maxMem -XstartOnFirstThread -jar $fatjar --envPort=$port
+java -Xmx$maxMem -XstartOnFirstThread \
+    -Dobsidianlink.disableClientAudio=true \
+    -jar $fatjar --envPort=$port
 [ $replaceable -gt 0 ]
 
--- a/src/main/java/net/minecraft/client/audio/SoundEngine.java
+++ b/src/main/java/net/minecraft/client/audio/SoundEngine.java
@@ -78,6 +78,11 @@ public class SoundEngine {
 
    private synchronized void load() {
       if (!this.loaded) {
+         if (Boolean.getBoolean("obsidianlink.disableClientAudio")) {
+            LOGGER.info(LOG_MARKER, "Sound engine disabled for ObsidianLink environment runtime");
+            return;
+         }
+
          try {
             this.sndSystem.init();
             this.listener.init();
