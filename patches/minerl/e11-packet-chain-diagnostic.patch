--- a/src/main/java/net/minecraft/client/multiplayer/PlayerController.java
+++ b/src/main/java/net/minecraft/client/multiplayer/PlayerController.java
@@ -271,6 +271,7 @@
             }
 
             this.connection.sendPacket(new CPlayerTryUseItemOnBlockPacket(p_217292_3_, p_217292_4_));
+            LOGGER.info("[E11-PACKET] client_send packet=CPlayerTryUseItemOnBlockPacket hand={} pos={} face={} state={} thread={} worldClass={} isRemote={}", p_217292_3_, blockpos, p_217292_4_.getFace(), p_217292_2_.getBlockState(blockpos), Thread.currentThread().getName(), p_217292_2_.getClass().getName(), p_217292_2_.isRemote());
             if (!itemstack.isEmpty() && !p_217292_1_.getCooldownTracker().hasCooldown(itemstack.getItem())) {
                 ItemUseContext itemusecontext = new ItemUseContext(p_217292_1_, p_217292_3_, p_217292_4_);
                 ActionResultType actionresulttype1;
--- a/src/main/java/net/minecraft/network/play/ServerPlayNetHandler.java
+++ b/src/main/java/net/minecraft/network/play/ServerPlayNetHandler.java
@@ -948,10 +948,12 @@
       BlockRayTraceResult blockraytraceresult = packetIn.func_218794_c();
       BlockPos blockpos = blockraytraceresult.getPos();
       Direction direction = blockraytraceresult.getFace();
+      LOGGER.info("[E11-PACKET] server_received handler=processTryUseItemOnBlock hand={} pos={} face={} state={} thread={} worldClass={} isRemote={}", hand, blockpos, direction, serverworld.getBlockState(blockpos), Thread.currentThread().getName(), serverworld.getClass().getName(), serverworld.isRemote());
       this.player.markPlayerActive();
       if (blockpos.getY() < this.server.getBuildLimit()) {
          if (this.targetPos == null && this.player.getDistanceSq((double)blockpos.getX() + 0.5D, (double)blockpos.getY() + 0.5D, (double)blockpos.getZ() + 0.5D) < 64.0D && serverworld.isBlockModifiable(this.player, blockpos)) {
             ActionResultType actionresulttype = this.player.interactionManager.func_219441_a(this.player, serverworld, itemstack, hand, blockraytraceresult);
+            LOGGER.info("[E11-PACKET] server_interaction_complete result={} pos={} state={} thread={} worldClass={} isRemote={}", actionresulttype, blockpos, serverworld.getBlockState(blockpos), Thread.currentThread().getName(), serverworld.getClass().getName(), serverworld.isRemote());
             if (direction == Direction.UP && !actionresulttype.isSuccessOrConsume() && blockpos.getY() >= this.server.getBuildLimit() - 1 && func_241166_a_(this.player, itemstack)) {
                ITextComponent itextcomponent = (new TranslationTextComponent("build.tooHigh", this.server.getBuildLimit())).mergeStyle(TextFormatting.RED);
                this.player.connection.sendPacket(new SChatPacket(itextcomponent, ChatType.GAME_INFO, Util.DUMMY_UUID));
--- a/src/main/java/net/minecraft/server/management/PlayerInteractionManager.java
+++ b/src/main/java/net/minecraft/server/management/PlayerInteractionManager.java
@@ -11,6 +11,7 @@
 import net.minecraft.inventory.container.INamedContainerProvider;
 import net.minecraft.item.ItemStack;
 import net.minecraft.item.ItemUseContext;
+import net.minecraft.item.FlintAndSteelItem;
 import net.minecraft.network.play.client.CPlayerDiggingPacket;
 import net.minecraft.network.play.server.SPlayerDiggingPacket;
 import net.minecraft.network.play.server.SPlayerListItemPacket;
@@ -313,6 +314,9 @@
 
          if (!stackIn.isEmpty() && !playerIn.getCooldownTracker().hasCooldown(stackIn.getItem())) {
             ItemUseContext itemusecontext = new ItemUseContext(playerIn, handIn, blockRaytraceResultIn);
+            if (stackIn.getItem() instanceof FlintAndSteelItem) {
+               field_225418_c.info("[E11-PACKET] server_player_interaction flint_enter pos={} state={} thread={} worldClass={} isRemote={}", blockpos, worldIn.getBlockState(blockpos), Thread.currentThread().getName(), worldIn.getClass().getName(), worldIn.isRemote());
+            }
             ActionResultType actionresulttype1;
             if (this.isCreative()) {
                int i = stackIn.getCount();
@@ -322,6 +326,9 @@
                actionresulttype1 = stackIn.onItemUse(itemusecontext);
             }
 
+            if (stackIn.getItem() instanceof FlintAndSteelItem) {
+               field_225418_c.info("[E11-PACKET] server_player_interaction flint_exit result={} pos={} state={} thread={} worldClass={} isRemote={}", actionresulttype1, blockpos, worldIn.getBlockState(blockpos), Thread.currentThread().getName(), worldIn.getClass().getName(), worldIn.isRemote());
+            }
             if (actionresulttype1.isSuccessOrConsume()) {
                CriteriaTriggers.RIGHT_CLICK_BLOCK_WITH_ITEM.test(playerIn, blockpos, itemstack);
             }
--- a/src/main/java/net/minecraft/item/FlintAndSteelItem.java
+++ b/src/main/java/net/minecraft/item/FlintAndSteelItem.java
@@ -12,8 +12,12 @@
 import net.minecraft.util.SoundEvents;
 import net.minecraft.util.math.BlockPos;
 import net.minecraft.world.World;
+import org.apache.logging.log4j.LogManager;
+import org.apache.logging.log4j.Logger;
 
 public class FlintAndSteelItem extends Item {
+   private static final Logger LOGGER = LogManager.getLogger();
+
    public FlintAndSteelItem(Item.Properties builder) {
       super(builder);
    }
@@ -24,6 +28,7 @@
       BlockPos blockpos = context.getPos();
       BlockState blockstate = world.getBlockState(blockpos);
       if (CampfireBlock.canBeLit(blockstate)) {
+         LOGGER.info("[E11-PACKET] flint_onItemUse campfire pos={} state={} thread={} worldClass={} isRemote={}", blockpos, blockstate, Thread.currentThread().getName(), world.getClass().getName(), world.isRemote());
          world.playSound(playerentity, blockpos, SoundEvents.ITEM_FLINTANDSTEEL_USE, SoundCategory.BLOCKS, 1.0F, random.nextFloat() * 0.4F + 0.8F);
          world.setBlockState(blockpos, blockstate.with(BlockStateProperties.LIT, Boolean.valueOf(true)), 11);
          if (playerentity != null) {
@@ -36,6 +41,7 @@
       } else {
          BlockPos blockpos1 = blockpos.offset(context.getFace());
          if (AbstractFireBlock.canLightBlock(world, blockpos1, context.getPlacementHorizontalFacing())) {
+            LOGGER.info("[E11-PACKET] flint_onItemUse light_fire pos={} state={} thread={} worldClass={} isRemote={}", blockpos1, world.getBlockState(blockpos1), Thread.currentThread().getName(), world.getClass().getName(), world.isRemote());
             world.playSound(playerentity, blockpos1, SoundEvents.ITEM_FLINTANDSTEEL_USE, SoundCategory.BLOCKS, 1.0F, random.nextFloat() * 0.4F + 0.8F);
             BlockState blockstate1 = AbstractFireBlock.getFireForPlacement(world, blockpos1);
             world.setBlockState(blockpos1, blockstate1, 11);
--- a/src/main/java/net/minecraft/block/AbstractFireBlock.java
+++ b/src/main/java/net/minecraft/block/AbstractFireBlock.java
@@ -17,8 +17,11 @@
 import net.minecraft.world.World;
 import net.minecraftforge.api.distmarker.Dist;
 import net.minecraftforge.api.distmarker.OnlyIn;
+import org.apache.logging.log4j.LogManager;
+import org.apache.logging.log4j.Logger;
 
 public abstract class AbstractFireBlock extends Block {
+   private static final Logger LOGGER = LogManager.getLogger();
    private final float fireDamage;
    protected static final VoxelShape shapeDown = Block.makeCuboidShape(0.0D, 0.0D, 0.0D, 16.0D, 1.0D, 16.0D);
 
@@ -122,7 +125,9 @@
 
    public void onBlockAdded(BlockState state, World worldIn, BlockPos pos, BlockState oldState, boolean isMoving) {
       if (!oldState.isIn(state.getBlock())) {
-         if (canLightPortal(worldIn)) {
+         boolean canLight = canLightPortal(worldIn);
+         LOGGER.info("[E11-PACKET] fire_onBlockAdded canLightPortal={} pos={} state={} thread={} worldClass={} isRemote={}", canLight, pos, worldIn.getBlockState(pos), Thread.currentThread().getName(), worldIn.getClass().getName(), worldIn.isRemote());
+         if (canLight) {
             Optional<PortalSize> optional = PortalSize.func_242964_a(worldIn, pos, Direction.Axis.X);
             if (optional.isPresent()) {
                optional.get().placePortalBlocks();
--- a/src/main/java/net/minecraft/block/PortalSize.java
+++ b/src/main/java/net/minecraft/block/PortalSize.java
@@ -153,8 +153,10 @@
 
    public void placePortalBlocks() {
       BlockState blockstate = Blocks.NETHER_PORTAL.getDefaultState().with(NetherPortalBlock.AXIS, this.axis);
+      System.out.println("[E11-PACKET] portal_place_enter axis=" + this.axis + " pos=" + this.bottomLeft + " state=" + this.world.getBlockState(this.bottomLeft) + " thread=" + Thread.currentThread().getName() + " worldClass=" + this.world.getClass().getName() + " isRemote=" + this.world.isRemote());
       BlockPos.getAllInBoxMutable(this.bottomLeft, this.bottomLeft.offset(Direction.UP, this.height - 1).offset(this.rightDir, this.width - 1)).forEach((p_242967_2_) -> {
          this.world.setBlockState(p_242967_2_, blockstate, 18);
+         System.out.println("[E11-PACKET] portal_place_cell pos=" + p_242967_2_ + " state=" + this.world.getBlockState(p_242967_2_) + " thread=" + Thread.currentThread().getName() + " worldClass=" + this.world.getClass().getName() + " isRemote=" + this.world.isRemote());
       });
    }
