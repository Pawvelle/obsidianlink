# MineRL 闲置归档

2026-08-22 起，ObsidianLink 的主动开发平台为 MineDojo。L1、D1、Portal benchmark、Agent 动作面和默认测试都已改走 `MineDojoEnvironment`。

仓库仍保留 `obsidianlink/env/minerl.py`，只为阅读旧实验记录。它不再被默认 API、runner 或 pytest 引用，也不应接收新功能。

地狱门 Benchmark 的研究目标仍然有效，并已经在 MineDojo open-ended 场景上继续实现。
